# packages

Home for shared helpers **extracted from starters once reuse is proven** — e.g. a published
`qa-kit-ts` with the base page, data factories, and config presets that multiple SUTs import.

Deliberately empty for now. Following prove-then-promote, helpers start life inside
`starters/playwright-ts/support/` and graduate here only when more than one place needs them.
Extracting earlier is premature abstraction.
